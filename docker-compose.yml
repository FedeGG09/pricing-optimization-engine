services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./out:/app/out:ro
      - ./artifacts:/app/artifacts
    restart: unless-stopped
